[project]
name = "cc-statusline"
version = "1.2.3"
description = "3-line Powerline statusline for Claude Code"
requires-python = ">=3.10"

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
